# Environment Setup

## Frontend
Copy `apps/web/.env.example` to `.env`.

## Backend
Copy `.env.example` to your local environment source.

## Notes
- Frontend expects backend on port 8080
- Backend expects PostgreSQL locally